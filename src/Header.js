import React from 'react'

class Header extends React.Component {
    render() {
        return (
            <div>
                <h1>API Interface</h1>
                <h3>Add user scores to the DynamoDB database and see those already inserted</h3>
            </div>
        )
     } 
 }

 export default Header